# 引き継ぎ書

- **作成日時**: 2026年7月27日（月）21:44:02
- **作成モデル / セッション**: Claude Fable 5（VSCode拡張、Claude Code）。途中 `/model sonnet` でSonnet 5に切替した区間あり
- **対象事業 / プロジェクト**: ZENKAI不動産株式会社（静岡市駿河区）のAI咨询新事業。母公司 株式会社ABCマテリアル。プロジェクトルート: `/Users/syubunhin/Developer/AI consultant service`（gitリポジトリ、branch: main）

## 1. 目的（何をやろうとしているか）

ZENKAIのAIコンサル新事業向けブランドサイト（全日本語・静的HTML）＋独立LPを構築する案件。研究→企画→実装→検収まで完了し、現在は**ユーザー最終確認〜本番公開前の仕上げ**の段階。今回のセッションでは主に細部修正と「友人に見せるための一時公開リンク作成」「VSCode拡張→デスクトップ版への作業引き継ぎ」を行った。

## 2. これまでにやったこと（理由つき）

※本セッション以前の詳細（V1→V2全面リニューアル、フォント/カラー設計、富士山SVGの試行錯誤、画像方針、V2.1色調調整など）は git log とこれ以前の会話サマリーに記録済み。ここでは**直近セッションで実際に行ったこと**を中心に記す。

- **補助金サービスの再定位**（前セッション、commit 5ff0107）: 自社は補助金の申請代行をしない方針に変更。理由：主要補助金は認定経営革新等支援機関の関与が事実上必須、かつ書類代作は行政書士法に抵触するリスクがあるとユーザーが判断したため。サイト全体（25ファイル）で「補助金申請サポート」→「補助金活用サポート」に改称し、`services/subsidy.html` に役割分担（当社＝制度整理・技術要件整理・専門家への橋渡し／提携専門家＝書類作成支援・申請手続き）を明記。「当社は申請書類の作成代行・申請代行は行いません」の一文を追加。提携候補は**株式会社ライツコンサルティング**（静岡県袋井市、代表 辻拓真氏、中小企業庁認定経営革新等支援機関 第74号、支援500社超・約30億円）と確認済みだが、**正式合意前のためサイト上には社名を出していない**（`services/subsidy.html` 内にHTMLコメントで掲載箇所の目印あり）。
- **サイトを友人に見せるための一時公開リンクを作成**: `gh` CLIが `syu-source` アカウントで既にログイン済みであることを確認。ユーザーの承認を得た上で、GitHub上に**パブリックリポジトリ** `zenkai-ai-site`（`https://github.com/syu-source/zenkai-ai-site`）を新規作成し、現在のmainブランチをそのままpush。GitHub Pagesを有効化し、以下のURLで一時公開中：
  - トップ: **https://syu-source.github.io/zenkai-ai-site/**
  - LP: **https://syu-source.github.io/zenkai-ai-site/lp/index.html**
  - HTTP 200を確認済み（動作確認済み）。
  - 注意：リポジトリは**公開設定**（GitHub Pages無料版の制約）。現状サイト内に実在する顧客情報は無く、電話/メール/Googleフォーム/地図はすべてプレースホルダーなので実害は無い判断だが、**本番ドメインではない・あくまで一時プレビュー用**である旨をユーザーに伝達済み。
- **VSCode拡張→デスクトップ版Claude Codeへの引き継ぎ方法を調査**: claude-code-guideエージェントに調査させた結果、①VSCode拡張とデスクトップ版はセッション履歴を共有しない（別々に管理）②ただしプロジェクト内の `CLAUDE.md` / `PROJECT_PLAN.md` / `worklog.md` はデスクトップ版でも自動的に読み込まれる③MCPサーバーとhooksの設定は `~/.claude/settings.json` に入っており両方から共通で見える、という回答を得た。**ただし出力に「instruction-shaped pattern」フラグが立っていたため鵜呑みにせず**、`~/.claude/` 配下を自分で直接確認。結果：ユーザー自身が既に `~/.claude/skills/handover/SKILL.md` と `~/.claude/skills/start/SKILL.md` という2つの個人用skillを用意済みであることを発見（agentの回答内容とも整合）。これを使うのが最も確実な引き継ぎ方法と判断し、本ファイルはその `/handover` skillの手順に沿って作成している。

## 3. 現在の状態

- サイト本体：**実装・検収済み**（PROJECT_PLAN.mdのPhase A〜E完了、全ページスクリーンショット目視確認済み、emoji=0確認済み、リンクチェック済み）。ただし三.jsのパーティクル背景はheadless環境の制約で**実ブラウザでの目視確認が未実施**（ユーザー側で確認推奨）。
- GitHub Pages公開：**動作確認済み**（HTTP 200、トップ・LP双方閲覧可能）。あくまで一時プレビュー用。
- 補助金サービスの提携専門家名：**未掲載（意図的）**。ライツコンサルティングとの正式合意待ち。
- 本番公開に向けた各種プレースホルダー：**未着手**（次項参照）。

## 4. 触ったファイル・場所（本セッション分）

- 新規: `handover/`フォルダ（このファイル用）
- 新規のGitHubリポジトリ: `https://github.com/syu-source/zenkai-ai-site`（ローカルの `origin` remoteとして追加済み。`git remote -v` で確認可）
- 本セッションではローカルファイルの内容変更は無し（前セッションの `5ff0107` 補助金再定位が最後のコード変更コミット）

## 5. 次の一手（具体的な順序で）

1. デスクトップ版Claude Codeで同じフォルダ（`/Users/syubunhin/Developer/AI consultant service`）を開き、最初のメッセージで `/start` を実行する（`~/.claude/skills/start/SKILL.md` が本ファイルを自動的に見つけて読み込む）。
2. ユーザーが友人からのフィードバックを受け取ったら、必要な修正を反映。
3. ライツコンサルティングとの提携合意が取れ次第、`services/subsidy.html` 内のHTMLコメント目印（「提携先確定後、ここに提携先名…を掲載する」）の箇所に正式に社名を掲載。
4. 本番公開前チェックリスト（PROJECT_PLAN.md #7・過去のサマリーに記載済み）: 代表者氏名・写真、電話番号、AI事業用メールアドレス、Googleフォーム URL ×2、TimeRex予約URL、Google Map埋め込み、実ドメイン取得（現状 example.com のまま）、`assets/ogp.png`、会社写真3枚（現状photo-slotプレースホルダー）の差し替え。
5. 本番公開後、`zenkai-ai-site` の一時公開用パブリックリポジトリは目的を終えたら**ユーザー自身の判断で**削除やprivate化を検討（現状はそのまま残っている）。

## 6. 未解決の点・注意・落とし穴

- **`zenkai-ai-site` リポジトリは現在パブリック設定のまま**残っている。本番運用に移る際、これを削除するかprivateにするか放置するかはユーザー判断待ち（自動では消していない）。
- three.jsのパーティクル背景はheadless screenshotでは検証不可のため、実ブラウザでの見た目未確認。
- VSCode拡張とデスクトップ版は**セッション履歴そのものは共有されない**。今回のように `/handover` → `/start` の橋渡しを使うのが確実な方法（プロジェクトを開くだけで自動的に会話が復元されるわけではない）。
- 本セッション中、GitHub上でのリポジトリ作成・Pages公開操作は「auto mode classifier」による許可待ちが一度発生し、ユーザーへの確認質問（AskUserQuestion）を経てから実行した。今後同様の公開系操作をする際も同様の確認フローが入る可能性がある。

## 7. 補足（次の担当が知っておくべきこと）

- プロジェクト規則は `/Users/syubunhin/Developer/AI consultant service/CLAUDE.md` に集約（中国語で対話・日本語でサイト文言、emoji全面禁止、実績捏造禁止、design-system.css改変禁止、補助金金額の注記義務、FDEはサブラベルのみ、未検証の完了報告は不可）。必ず最初に読むこと。
- 事実基準（運営主体住所・母公司情報など）も同CLAUDE.mdの「事実基準」セクションに記載。ここは記憶で書き換えないこと。
- `worklog.md` に変更履歴が時系列で残っているので、詳細な経緯（富士山SVGの試行錯誤、画像方針の意思決定、データ出典の訂正履歴など）はそちらを参照。
- `gh` CLIは `syu-source` アカウントでログイン済み・repo権限あり。デスクトップ版でも同じマシンなら認証状態は共有されるはず。
